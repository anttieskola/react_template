/**
 * Run action creators you want to be run application startup here.
 * These are run after store initialized and state loaded from
 * browser storage if it was found.
 *
 * These are meant for example to refresh/acquire data from server
 * or initialize other libraries that are used globally.
 * @param  {object} store - redux store
 */
export const applicationInit = (store) => {
  // TODO: dispatch actions
};

export default applicationInit;
